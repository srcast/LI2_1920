/**
 * @file interface.h
 * @brief Componente que interage com o sistema (SGV) e utilizador.
 * 
 * Trata e valida o input do utilizador, efetuar a comunicação necessária
 * com o sistema e passa à View a informação que deve ser mostrada ao utilizador
 */
#ifndef INTERFACE_H
#define INTERFACE_H

/**
 * @brief Inicializa o fluxo do programa.
 */
void initController();

#endif